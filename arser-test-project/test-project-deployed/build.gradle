plugins {
    id "java"
}

description = "Arser Test Project (Deployed)"

dependencies {
    // implementation(project(":test-project"))

    // Force to load from Repository.
    implementation("de.freese.arser:test-project:$version")
}
